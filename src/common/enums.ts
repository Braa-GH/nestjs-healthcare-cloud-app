export enum DbTypes {
    postgres = "postgres"
}

export enum Sex {
    male = "male",
    female = "female"
}

export enum Roles {
    Admin = "Admin",
    Doctor = "Doctor",
    Patient = "Patient",
    User = "User",
    Owner = "Owner"
}

export enum AppointmentStatus {
    Accepted = "accepted",
    Rejected = "rejected",
    Waiting = "waiting",
    Canceled = "canceled"
}