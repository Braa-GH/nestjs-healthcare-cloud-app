export enum DbTypes {
    postgres = "postgres",
    mysql = "mysql",
}

export enum Sex {
    male = "male",
    female = "female"
}

export enum Roles {
    Admin = "Admin",
    Doctor = "Doctor",
    Patient = "Patient",
    User = "User",
    Owner = "Owner"
}

export enum AppointmentStatus {
    Accepted = "accepted",
    Rejected = "rejected",
    Waiting = "waiting",
    Canceled = "canceled"
}

export enum ApplicationStatus {
    Accepted = "accepted",
    Rejected = "rejected",
    Waiting = "waiting",
}