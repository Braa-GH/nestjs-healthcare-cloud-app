export type UserIdentifiers = {
    id?: string;
    email?: string
}

export type AdminIdentifiers = {
    id?: string;
    userId?: string
}

export type PatientIdentifiers = {
    id?: string;
    userId?: string
}